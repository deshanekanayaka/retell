# <Feature Name> Spec

## Overview

One paragraph: what this is and why now. Cite the requirements it implements by FR number, for
example "implements docs/01-PRD.md FR-16 and FR-17".

## Requirements

Testable MUST behaviours, one per line.

- MUST ...

## Out of scope

What this deliberately does not do.

## Implementation notes

Constraints, chosen approach, tables and files touched. Reference `docs/` rather than restating
it.

## Test plan

What proves this works. Unit cases, and the calibration impact if anything touching the rubric
or the scheduler changes.

## Docs impacted

Which `docs/` files this makes stale and must be updated before completion. Can be "none".
